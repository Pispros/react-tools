import * as sampleTypes from '../constants/sample.types.js'

const initialState = {
    
}

export const sampleReducer = (state = initialState, action) => {

    let copyState = {...state} ;

    switch (action.type)
    {
        
    }
    return copyState;
}